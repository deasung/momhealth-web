version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - npm ci
  build:
    commands:
      - cp .env.production .env
      - NODE_ENV=production npm run build
artifacts:
  files:
    - '.next/**/*'
    - '.env'
    - 'public/**/*'
    - 'scripts/**/*'
    - 'package.json'
    - 'package-lock.json'
    - 'next.config.js'
    - 'appspec.yml'
  discard-paths: no